console.log('12' < '9') //returns true because as a string it only conpared the '1' to the '9' and not the whole number.
console.log(12 < 9)    //returns false because we are now comparing numbers and not strings.